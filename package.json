{
  "name": "global-ping",
  "version": "1.0.0",
  "description": "Global ping tool using Cloudflare Workers and Durable Objects",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "wrangler": "^4.10.0",
    "typescript": "^5.0.4"
  }
}